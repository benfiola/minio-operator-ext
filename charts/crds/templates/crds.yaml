---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: miniobuckets.bfiola.dev
spec:
  group: bfiola.dev
  names:
    kind: MinioBucket
    listKind: MinioBucketList
    plural: miniobuckets
    singular: miniobucket
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Tenant Namespace
      jsonPath: .status.currentSpec.tenantRef.namespace
      name: Tenant Namespace
      type: string
    - description: Tenant Name
      jsonPath: .status.currentSpec.tenantRef.name
      name: Tenant Name
      type: string
    - description: Name
      jsonPath: .status.currentSpec.name
      name: Name
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: MinioBucket defines a MinIO tenant bucket.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinioBucketSpec defines the desired state of MinioBucket
            properties:
              deletionPolicy:
                type: string
              name:
                type: string
              tenantRef:
                description: ResourceRef defines a reference to another kubernetes
                  resource
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
            required:
            - deletionPolicy
            - name
            - tenantRef
            type: object
          status:
            description: MinioBucketStatus defines the current state of MinioBucket
            properties:
              currentSpec:
                description: MinioBucketSpec defines the desired state of MinioBucket
                properties:
                  deletionPolicy:
                    type: string
                  name:
                    type: string
                  tenantRef:
                    description: ResourceRef defines a reference to another kubernetes
                      resource
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - deletionPolicy
                - name
                - tenantRef
                type: object
              synced:
                type: boolean
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: miniogroups.bfiola.dev
spec:
  group: bfiola.dev
  names:
    kind: MinioGroup
    listKind: MinioGroupList
    plural: miniogroups
    singular: miniogroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Tenant Namespace
      jsonPath: .status.currentSpec.tenantRef.namespace
      name: Tenant Namespace
      type: string
    - description: Tenant Name
      jsonPath: .status.currentSpec.tenantRef.name
      name: Tenant Name
      type: string
    - description: Name
      jsonPath: .status.currentSpec.name
      name: Name
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: MinioGroup defines a MinIO builtin group identity.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinioGroupSpec defines the desired state of MinioGroup
            properties:
              name:
                type: string
              tenantRef:
                description: ResourceRef defines a reference to another kubernetes
                  resource
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
            required:
            - name
            - tenantRef
            type: object
          status:
            description: MinioGroupStatus defines the current state of MinioGroup
            properties:
              currentSpec:
                description: MinioGroupSpec defines the desired state of MinioGroup
                properties:
                  name:
                    type: string
                  tenantRef:
                    description: ResourceRef defines a reference to another kubernetes
                      resource
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - name
                - tenantRef
                type: object
              synced:
                type: boolean
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: miniogroupbindings.bfiola.dev
spec:
  group: bfiola.dev
  names:
    kind: MinioGroupBinding
    listKind: MinioGroupBindingList
    plural: miniogroupbindings
    singular: miniogroupbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Tenant Namespace
      jsonPath: .status.currentSpec.tenantRef.namespace
      name: Tenant Namespace
      type: string
    - description: Tenant Name
      jsonPath: .status.currentSpec.tenantRef.name
      name: Tenant Name
      type: string
    - description: User
      jsonPath: .status.currentSpec.user
      name: User
      type: string
    - description: Group
      jsonPath: .status.currentSpec.group
      name: Group
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: MinioGroupBinding attaches a MinIO builtin user identity to a
          MinIO builtin group identity
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinioGroupBindingSpec defines the desired state of MinioGroupBinding
            properties:
              group:
                type: string
              tenantRef:
                description: ResourceRef defines a reference to another kubernetes
                  resource
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              user:
                type: string
            required:
            - group
            - user
            type: object
          status:
            description: MinioGroupBindingStatus defines the current state of MinioGroupBinding
            properties:
              currentSpec:
                description: MinioGroupBindingSpec defines the desired state of MinioGroupBinding
                properties:
                  group:
                    type: string
                  tenantRef:
                    description: ResourceRef defines a reference to another kubernetes
                      resource
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                  user:
                    type: string
                required:
                - group
                - user
                type: object
              synced:
                type: boolean
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: miniopolicies.bfiola.dev
spec:
  group: bfiola.dev
  names:
    kind: MinioPolicy
    listKind: MinioPolicyList
    plural: miniopolicies
    singular: miniopolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Tenant Namespace
      jsonPath: .status.currentSpec.tenantRef.namespace
      name: Tenant Namespace
      type: string
    - description: Tenant Name
      jsonPath: .status.currentSpec.tenantRef.name
      name: Tenant Name
      type: string
    - description: Name
      jsonPath: .status.currentSpec.name
      name: Name
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: MinioPolicy defines a MinIO tenant policy
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinioPolicySpec defines the desired state of MinioPolicy
            properties:
              name:
                type: string
              statement:
                items:
                  description: MinioPolicyStatement defines a single statement within
                    a MinioPolicy
                  properties:
                    action:
                      items:
                        type: string
                      type: array
                    effect:
                      type: string
                    resource:
                      items:
                        type: string
                      type: array
                  required:
                  - action
                  - effect
                  - resource
                  type: object
                type: array
              tenantRef:
                description: ResourceRef defines a reference to another kubernetes
                  resource
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              version:
                type: string
            required:
            - name
            - statement
            - tenantRef
            - version
            type: object
          status:
            description: MinioPolicyStatus defines the current state of MinioPolicy
            properties:
              currentSpec:
                description: MinioPolicySpec defines the desired state of MinioPolicy
                properties:
                  name:
                    type: string
                  statement:
                    items:
                      description: MinioPolicyStatement defines a single statement
                        within a MinioPolicy
                      properties:
                        action:
                          items:
                            type: string
                          type: array
                        effect:
                          type: string
                        resource:
                          items:
                            type: string
                          type: array
                      required:
                      - action
                      - effect
                      - resource
                      type: object
                    type: array
                  tenantRef:
                    description: ResourceRef defines a reference to another kubernetes
                      resource
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    type: string
                required:
                - name
                - statement
                - tenantRef
                - version
                type: object
              synced:
                type: boolean
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: miniopolicybindings.bfiola.dev
spec:
  group: bfiola.dev
  names:
    kind: MinioPolicyBinding
    listKind: MinioPolicyBindingList
    plural: miniopolicybindings
    singular: miniopolicybinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Tenant Namespace
      jsonPath: .status.currentSpec.tenantRef.namespace
      name: Tenant Namespace
      type: string
    - description: Tenant Name
      jsonPath: .status.currentSpec.tenantRef.name
      name: Tenant Name
      type: string
    - description: Policy
      jsonPath: .status.currentSpec.policy
      name: Policy
      type: string
    - description: User
      jsonPath: .status.currentSpec.user
      name: User
      type: string
    - description: Group
      jsonPath: .status.currentSpec.group
      name: Group
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: MinioPolicyBinding attaches a MinIO identity to a MinIO policy
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinioPolicyBindingSpec defines the desired state of MinioPolicyBinding
            properties:
              group:
                properties:
                  builtin:
                    type: string
                  ldap:
                    type: string
                type: object
              policy:
                type: string
              tenantRef:
                description: ResourceRef defines a reference to another kubernetes
                  resource
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              user:
                properties:
                  builtin:
                    type: string
                  ldap:
                    type: string
                type: object
            required:
            - policy
            - tenantRef
            type: object
          status:
            description: MinioPolicyBindingStatus defines the current state of MinioPolicyBinding
            properties:
              currentSpec:
                description: MinioPolicyBindingSpec defines the desired state of MinioPolicyBinding
                properties:
                  group:
                    properties:
                      builtin:
                        type: string
                      ldap:
                        type: string
                    type: object
                  policy:
                    type: string
                  tenantRef:
                    description: ResourceRef defines a reference to another kubernetes
                      resource
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                  user:
                    properties:
                      builtin:
                        type: string
                      ldap:
                        type: string
                    type: object
                required:
                - policy
                - tenantRef
                type: object
              synced:
                type: boolean
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: miniousers.bfiola.dev
spec:
  group: bfiola.dev
  names:
    kind: MinioUser
    listKind: MinioUserList
    plural: miniousers
    singular: miniouser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Tenant Namespace
      jsonPath: .status.currentSpec.tenantRef.namespace
      name: Tenant Namespace
      type: string
    - description: Tenant Name
      jsonPath: .status.currentSpec.tenantRef.name
      name: Tenant Name
      type: string
    - description: Access Key
      jsonPath: .status.currentSpec.accessKey
      name: Access Key
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: MinioUser defines a MinIO builtin user identity.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinioUserSpec defines the desired state of MinioUser
            properties:
              accessKey:
                type: string
              secretKeyRef:
                description: ResourceKeyRef defines a reference to a key of another
                  kubernetes resource
                properties:
                  key:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - key
                - name
                type: object
              tenantRef:
                description: ResourceRef defines a reference to another kubernetes
                  resource
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
            required:
            - accessKey
            - secretKeyRef
            - tenantRef
            type: object
          status:
            description: MinioUserStatus defines the current state of MinioUser
            properties:
              currentSecretKeyRefResourceVersion:
                type: string
              currentSpec:
                description: MinioUserSpec defines the desired state of MinioUser
                properties:
                  accessKey:
                    type: string
                  secretKeyRef:
                    description: ResourceKeyRef defines a reference to a key of another
                      kubernetes resource
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  tenantRef:
                    description: ResourceRef defines a reference to another kubernetes
                      resource
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - accessKey
                - secretKeyRef
                - tenantRef
                type: object
              synced:
                type: boolean
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
